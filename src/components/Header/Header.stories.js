import { Header } from "./Header";

export default {
  title: "Components/Header",
  component: Header,
};

const Template = () => <Header>App Header</Header>;

export const Default = Template.bind({});
